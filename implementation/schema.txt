User {
    email,
    pw_hash,
    default_config,
    created_on
}

Config {
    user_id,
    time_format,
    am_pm,
    show_seconds
}

Devices {
    uid,
    user_id,
    config_id
}

Calendar {
    user_id,
    calendar_id
}